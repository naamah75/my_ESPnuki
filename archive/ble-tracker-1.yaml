substitutions:
  friendly_name: BLE tracker 1
  devicename: ble-tracker-1

esphome:
  name: ${devicename}
  friendly_name: ${friendly_name}

esp32:
  board: lolin_d32
  framework:
    type: esp-idf
logger:
  level: DEBUG

debug: null

api:
  encryption:
    key: OHjuhrQkj25towWG46NtTk1Xbf0k5B+NGzUfqn4hQ7g=

wifi:
  ssid: !secret wifi_ssid_2
  password: !secret wifi_password_2
  ap:
    ssid: ${devicename} Fallback Hotspot
    password: yK4Nia32K90Z

ota:
- platform: esphome
  password: b226988b7f381d8e92bfc08b827a6b9e

captive_portal: null

web_server:
  version: 3

status_led:
  pin:
    number: GPIO5
    inverted: true

esp32_ble_tracker:
  scan_parameters:
    active: true
    interval: 211ms
    window: 120ms

bluetooth_proxy:
  active: true

text_sensor:
- platform: wifi_info
  ip_address:
    name: ${devicename} IP address
    icon: mdi:access-point-network
  ssid:
    name: ${devicename} SSID
    icon: mdi:access-point-network
  bssid:
    name: ${devicename} BSSID
    icon: mdi:access-point-network
  mac_address:
    name: ${devicename} MAC address
    icon: mdi:chip

switch:
- platform: restart
  name: ${friendly_name} restart
  device_class: switch
  entity_category: diagnostic
  
sensor:
- platform: wifi_signal
  name: ${devicename} WiFi Signal
  id: wifi_signal_db
  entity_category: diagnostic
  device_class: signal_strength
  icon: mdi:wifi
  update_interval: 60s
- platform: uptime
  name: ${devicename} Uptime
  icon: mdi:clock
  device_class: duration
  update_interval: 60s
- platform: debug
  free:
    name: ${friendly_name} heap free
    entity_category: diagnostic
    device_class: data_size
    icon: mdi:bug
    id: energy_monitor_esp32s3_heap_free
  block:
    name: ${friendly_name} heap max block
    entity_category: diagnostic
    device_class: data_size
    icon: mdi:bug
    id: energy_monitor_esp32s3_heap_max_block
  loop_time:
    name: ${friendly_name} loop time
    entity_category: diagnostic
    device_class: duration
    icon: mdi:bug
    id: energy_monitor_esp32s3_loop_time
- platform: uptime
  name: ${devicename} Uptime