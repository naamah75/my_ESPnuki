# Ignore everything in this ESPHome workspace by default.
/*

# Keep the ignore rules themselves tracked.
!/.gitignore
!/README.md

# Keep only the door controller configs/docs in this repo.
!/ibeacon-detector*.*
